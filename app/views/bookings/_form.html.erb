<%= form_for(booking) do |f| %>
  <% if booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(booking.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
      <% booking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :room_id %>
    <%= f.text_field :room_id %>
  </div>

  <div class="field">
    <%= f.label :from %>
    <%= f.date_select :from %>
  </div>

  <div class="field">
    <%= f.label :to %>
    <%= f.date_select :to %>
  </div>

  <div class="field">
    <%= f.label :hasCheckedOut %>
    <%= f.check_box :hasCheckedOut %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
